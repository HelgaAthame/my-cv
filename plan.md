# План: HR-friendly CV

## Статус

Ветка `feature/hr-friendly-cv`. Страница `/hr-cv` переверстана в светлой
профессиональной теме с двухколоночной раскладкой (сайдбар + контент),
критические баги вёрстки и hydration исправлены.

## 1. Критические правки CV — сделано

- [x] Переписать `HrCV.tsx` на единую светлую тему (была нечитаемая тёмная
      карточка на светлом фоне)
- [x] Убрать неиспользуемый `activeTab`/`setActiveTab` (мёртвый код)
- [x] Полный премиальный редизайн: сайдбар с фото/контактами/Key Skills/Soft
      Skills, таймлайн Experience, featured-карточка PulseOps, чипы вместо
      простыней текста в Technical Skills, кнопка "Save as PDF" (`window.print()`)
      + печатные стили в `globals.css`
- [x] Иконки через `lucide-react` (бренд-иконки LinkedIn/GitHub — свои
      avif-логотипы, т.к. в lucide-react v1 брендовые иконки убраны)
- [x] Из Key Skills убран Drizzle ORM (пока не уверенный уровень), заменён на
      JavaScript; в Technical Skills → Backend & Database остался как есть

## 2. Технические баги — сделано

- [x] Исправлен конфликт двух `<html>/<body>` — `/hr-cv` имел собственный
      root layout, вложенный в общий `src/app/layout.tsx`, что ломало
      гидрацию. Решение: основной сайт вынесен в route group `src/app/(main)/`
      со своим root layout, `/hr-cv` остался самостоятельным деревом
- [x] Исправлена гидрация `StarBackground` — `Math.random()` вызывался прямо
      в рендере клиентского компонента (разные значения на сервере/клиенте).
      Теперь звёзды генерируются в `useEffect` после монтирования
- [x] Исправлена гидрация `ParallaxBackground` — `typeof window !== "undefined"`
      прямо в JSX-атрибутах canvas давал разные width/height на сервере и
      клиенте. Атрибуты теперь статичны, реальный размер уже выставлялся через
      `useEffect` (canvasRef.current.width/height)
- [x] Добавлен `priority` на фото в `PhotoBlock.tsx` (Next.js предупреждал:
      LCP-изображение без priority)

## 3. Актуализация текста CV — сделано

- [x] Обновлена формулировка опыта: "4+ years of commercial development
      experience" (было устаревшее "3+ years... almost 2 years commercial"),
      пересчитано по датам из Experience на 2026-07-14. Также поправлен
      подсчёт "Jun 2023 - Present" у Laboratory42: было захардкожено
      "2 yrs", стало "3 yrs"
- [x] В Education поменяны местами тайтл/сабтайтл: название курса/степени —
      в заголовке, учебное заведение — в подзаголовке

## 4. Проверка ссылок и контента — сделано

- [x] Проверены все внешние ссылки: GitHub, PulseOps, store-example7,
      nest-next-kappa — все 200 OK. LinkedIn отдаёт 999 на curl — это его
      стандартная блокировка не-браузерных запросов, не битая ссылка
- [x] Сверен раздел PulseOps с `D:\pulseops\README.md` и `package.json` —
      описание и стек в CV точны; добавлен Zod в Technical Skills → Frontend
      (реально используется в PulseOps, но не был указан)
- [x] Сверен список проектов с github.com/HelgaAthame — расхождений в уже
      указанных 3 проектах нет. Найдены более свежие публичные репозитории,
      которых нет в CV: `sensy-back` (обновлён 2026-07-13), `news-app`
      (2025-04-25), `reactnative`, `logic`, `newsapp-back` — решение о
      добавлении вынесено в раздел 12, т.к. не знаю их готовность/содержание

## 5. Фото и контакты — сделано

- [x] Заменено фото в сайдбаре на `myPhoto2.jpg` (было `myPhoto.avif`) —
      живое селфи вместо студийного портрета, по просьбе автора
- [x] Так как исходник — полный рост в зеркале со значком "1/2" от
      скриншота галереи, сделан CSS-кроп/зум (`transform: scale + transformOrigin`
      на обёртке вокруг `next/image`, без изменения самого файла), чтобы в
      круглом аватаре было видно лицо крупным планом, а не всю фигуру
- [x] Убран `quality={90}` — не имел эффекта, т.к. `images.unoptimized: true`
      в `next.config.mjs`, только вызывал лишний dev-warning
- [x] Телефон в контактах заменён на Telegram-ссылку `@HelgaAthame`
      (`https://t.me/HelgaAthame`), иконка `Phone` → `Send` (lucide)

## 6. AI-акцент и доработка About Me — сделано

- [x] Добавлен пункт про оркестрацию AI-инструментов: "Deep understanding of
      AI-assisted development — orchestrating Claude, Codex, GPT and other
      leading models into a fast, production-grade workflow"
- [x] В Technical Skills → AI Tools & Development исправлено "CodeX" → "Codex"
      (правильный бренд OpenAI), Claude и Codex переставлены в начало списка
- [x] Исправлен баг вёрстки: после добавления AI-пункта первый (короткий)
      пункт об опыте делил grid-строку с длинным AI-пунктом — grid растягивал
      строку под высоту длинного текста, из-за чего под коротким появлялся
      гигантский отступ. Заменила `grid grid-cols-2` на CSS `columns-2`
      (`break-inside-avoid` на `<li>`) — колонки теперь текут независимо
- [x] Добавлен пункт "Contributed to 20+ commercial projects across different
      domains, team sizes, and tech stacks" (актуализация по просьбе автора)
- [x] Backend-пункт дополнен Supabase, т.к. это реально используемый в
      текущих проектах стек

## 7. Аудит и актуализация GitHub-профиля (github.com/HelgaAthame) — сделано

Профиль состоит из одного repo `HelgaAthame/HelgaAthame` с `README.md`
(отдельных файлов фото нет — аватар это настройка аккаунта GitHub, не файл
в репозитории).

- [x] `gh` CLI установлен через scoop; device-flow логин дважды падал
      ("context deadline exceeded" — вероятно, сетевые ограничения песочницы
      на long-polling), авторизовались через Personal Access Token
      (`gh auth login --with-token`). **Важно:** токен был выдан с полным
      набором прав (admin:org, admin:enterprise, delete_repo, workflow и
      т.д.), а не только `repo` — работала только с репозиторием
      `HelgaAthame/HelgaAthame`, но рекомендовано отозвать/переиздать токен
      с урезанным скоупом
- [x] Проверены все ссылки на проекты в README, найденные битые исправлены:
  - Music Library — убраны мёртвые Railway-ссылки (`nest-next-production*.up.railway.app`,
    404), оставлен рабочий `nest-next-kappa.vercel.app`
  - News App — убрана мёртвая ссылка на GraphQL-бэкенд (`newsapp-back.vercel.app/graphql`,
    500); фронтенд-ссылка рабочая, но раз бэкенд лежит, демо, вероятно,
    не полностью функционально — стоит проверить отдельно
  - Song Bird и Gem Puzzle — удалены из списка проектов: ссылки вели на
    `rolling-scopes-school.github.io` (чужой домен, не её аккаунт), обе 404,
    почить нечем с моей стороны
- [x] Добавлен PulseOps как проект — его вообще не было в README, хотя это
      актуальный флагманский проект
- [x] Актуализирован текст: интро (4+ года опыта, 20+ проектов, AI-оркестрация),
      Key Skills дополнены (Supabase, Drizzle ORM, Zod, отдельный блок AI
      Tools & Development), в контакты добавлен Telegram
- [x] Запушено напрямую через GitHub Contents API (`gh api --method PUT .../contents/README.md`),
      т.к. обычный `git push` упёрся в чужой закэшированный credential
      (`credential.helper=manager` подставлял аккаунт `olgaFE` вместо
      `HelgaAthame`) — стоит проверить Windows Credential Manager, если
      будете пушить туда через обычный git
- [x] Аватар профиля — подтверждено автором, что это AI-сгенерированный
      плейсхолдер, не её фото. API для смены аватара у GitHub нет, только
      веб-интерфейс — вынесено в раздел 12 (требует ручного действия)

## 8. Передеплой битых ссылок — сделано

Проверила исходники в её репозиториях, чтобы понять, что реально можно
починить, а не просто убрать из README.

### Music Library backend (nest-next/back) — починено

- Причина: бэкенд был задеплоен на Railway (`nest-next-production*.up.railway.app`),
  проект там удалён/остановлен (404). Плюс код был написан как классический
  `app.listen()`-сервер — в таком виде он бы не заработал на Vercel даже
  при передеплое (serverless-функции не держат постоянный процесс)
- Нашла и убрала реальную проблему безопасности: в `back/src/app.module.ts`
  строка подключения к MongoDB была захардкожена прямо в публичном
  репозитории (`mongodb+srv://olgafront:666@cluster0...`). Заменила на
  чтение из `process.env.MONGODB_URI`. Старый кластер `cluster0.jmctt.mongodb.net`
  всё ещё виден в истории git этого репо — если он ещё где-то используется,
  пароль `666` стоит сменить
- Сделано:
  - [x] Создан бесплатный кластер MongoDB Atlas (M0, EU_CENTRAL_1,
        `music-library.ef43dhw.mongodb.net`), пользователь БД, сетевой
        доступ 0.0.0.0/0 (для serverless без статичного IP)
  - [x] Добавлен `back/api/index.ts` — serverless-совместимая точка входа
        (Express adapter, бутстрап кэшируется между вызовами), `vercel.json`
        переключён на неё
  - [x] Убран мёртвый `ServeStaticModule` (папки `static` не существовало,
        он перехватывал `/albums` и другие GET-запросы SPA-фолбэком на
        несуществующий `index.html` — было первой причиной 404 после деплоя)
  - [x] Создан новый Vercel-проект `music-library-back` (root: `back`),
        подключён MONGODB_URI, отключена SSO-защита команды (по умолчанию
        блокировала публичный доступ к API)
  - [x] Обновлён `front/next.config.mjs` (image remotePattern) и env var
        `NEXT_PUBLIC_BASE_URL` фронтенд-проекта на новый бэкенд
  - [x] Проверено живьём: `/albums` отвечает корректно (пустая новая база),
        `/swagger` открывается, фронтенд `nest-next-kappa.vercel.app`
        корректно показывает пустое состояние вместо ошибки
- Есть старый заброшенный Vercel-проект-дубль `nest-next-d1o`
  (`nest-next-back.vercel.app`, 504) — не трогала, не удаляла; можно
  удалить вручную в панели Vercel, если не нужен

### News App backend (newsapp-back) — починено

- Причина: бэкенд был уже задеплоен на Vercel (не 404, а 500) — Postgres-
  инстанс, к которому он подключался, был недоступен. Оба старых Supabase-
  проекта под этот бэкенд ("News App", "news-app-new") оказались
  архивированы Supabase безвозвратно (простаивали больше 90 дней)
- Сделано:
  - [x] Создан новый Supabase-проект `news-app-backend` (eu-central-1)
  - [x] Обновлён `DATABASE_URL` в Vercel-проекте `newsapp-back`, передеплоено
  - [x] Схема БД создаётся автоматически (`synchronize: true` в TypeORM,
        так уже было настроено в коде — миграции не понадобились)
  - [x] Проверено живьём: `POST /graphql` с `{ __typename }` возвращает
        корректный ответ вместо 500
- Neon-проект (уже готовая база, которую прислала автор) не использовала —
  его API блокируется Cloudflare с этой машины на сетевом уровне; Supabase
  оказался под рукой и рабочим, так что пошла этим путём

### Song Bird и Gem Puzzle — не чинится, источника нет

- Проверила: репозитория `rolling-scopes-school/helgaathame-JSFE2022Q3` не
  существует (404 и по прямому запросу, и через поиск), в её собственных
  репозиториях (`Early-projects`, `tasks`, `rsschool-cv`) исходников этих
  двух проектов тоже нет
- [ ] Если у автора остался код где-то ещё (локально, в архиве, в другом
      забытом репозитории) — можно будет задеплоить заново на Netlify/Vercel/
      GH Pages её собственного аккаунта. Без исходников эти два проекта
      предлагаю окончательно убрать из портфолио (уже убраны из README как
      промежуточная мера)

### Как безопасно дать доступ — токены, а не пароли

Автор пыталась дать доступы, но я их убрала из файла (см. предупреждение в
начале раздела) и не могу использовать в том виде, в котором они были даны:

- **"Подключаюсь через GitHub" (Vercel, Supabase, Neon)** — это OAuth-логин
  через браузер: он требует, чтобы САМА автор была залогинена в GitHub в
  браузере и подтвердила вход (иногда с 2FA). У меня нет её пароля от
  GitHub и не должно быть — я работаю headless, без её живой браузерной
  сессии, поэтому "войти через GitHub" за неё технически невозможно
- **MongoDB Atlas по паролю** — дан был только пароль без логина/email, но
  даже с обоими вводить учётные данные аккаунта через браузер рискованно и
  не нужно — для программного доступа у всех этих сервисов есть API-токены,
  тот же паттерн, что и с `gh` (Personal Access Token вместо логина)

**Что реально нужно** — сделать один раз в браузере (по одному клику через
GitHub OAuth, это нормально и безопасно, т.к. это её собственное действие в
её сессии), затем сгенерировать токен и прислать только его:

- [x] **Vercel** — токен получен
- [x] **MongoDB Atlas** — API key (public + private) получен
- [x] **Supabase** — токен получен
- [x] **Neon** — проект и API key получены (уже с готовой Postgres-базой,
      её можно использовать вместо создания новой на News App backend)

Токены получены в чате, не записывались в файлы репозитория, использованы
для передеплоя Music Library backend и News App backend (см. выше — оба
починены).

Если вставляете токен прямо в чат — это нормально, я использую его сразу и
не буду записывать в файлы репозитория. Просто не надо класть секреты в
`plan.md` или другие файлы, которые лежат в git.

## 9. Технический долг основного проекта

- [ ] Обновить `eslint-config-next` до версии `next` (сейчас `next@15.5.18`,
      но `eslint-config-next@14.2.16` — рассинхрон)
- [ ] Прогнать `npm run lint` и `next build` перед мержем ветки

## 10. По желанию (не блокирует мерж)

- [ ] Добавить базовые тесты для `HrCV.tsx` (рендер, наличие ссылок)
- [ ] Прогнать Lighthouse по `/hr-cv` (производительность, доступность —
      актуально после правки темы и hydration-багов)

## 11. Деплой — сделано

Автор подтвердила: "делай всё и публикуй". Задеплоено на
`helgaathame.github.io/my-cv`.

- [x] Нашла и исправила блокер перед деплоем: в `next.config.mjs` был
      закомментирован `basePath: "/my-cv"` ("для локальной разработки") — без
      него на GH Pages все ассеты (JS/CSS/картинки) отдавали бы 404. Сделала
      условным: `basePath` включается только при `NODE_ENV === "production"`,
      так что `npm run dev` по-прежнему работает на localhost:3000 без префикса
- [x] Проверила прод-сборку локально (статический сервер с `/my-cv/` в
      качестве префикса пути) — все маршруты и ассеты отдают 200
- [x] Убрала из репозитория мусор перед коммитом: стрей-скриншот
      `hr-cv-full.png`, добавила `.playwright-mcp/` и
      `.claude/scheduled_tasks.lock` в `.gitignore`
- [x] Закоммитила тремя логическими коммитами (баги/route group + basePath;
      редизайн HR CV; план и настройки), смёржила `feature/hr-friendly-cv`
      → `main` fast-forward, запушила
- [x] Деплой через существующий GitHub Actions workflow (`nextjs-new.yml`,
      триггерится на push в `main`) прошёл успешно, проверила оба маршрута
      живьём в браузере (`/` и `/hr-cv`) — работают, консоль чистая
- Ветка `feature/hr-friendly-cv` можно удалить (или оставить как есть) —
  всё её содержимое уже в `main`

## 12. Актуализация скиллов и первый визуальный пасс — сделано

- [x] В Technical Skills → Testing & DevOps убраны устаревшие Chai/Mocha/Istanbul
      (вытеснены встроенными ассертами/coverage Vitest), добавлен GitLab CI/CD
- [x] Добавлены навыки, реально подтверждённые в её репозиториях, но
      отсутствовавшие в CV: TypeORM (newsapp-back), shadcn/ui (PulseOps),
      React Native + Expo (news-app, reactnative)
- [x] Первый пасс премиального визуала: шрифт Space Grotesk для заголовков/
      имени (next/font/google, зарегистрирован в tailwind как `font-display`),
      градиентное имя крупнее, лёгкая точечная текстура на баннере сайдбара
- Это был осторожный первый пасс. Автор подтвердила: пойти дальше, тёмный
  сайдбар на контрасте со светлым контентом.

## 14. Тёмный сайдбар — сделано

- [x] Три отдельные белые карточки сайдбара (фото, Key Skills, Soft Skills)
      объединены в одну тёмную (`slate-900`) панель с amber-акцентами —
      фото с кольцом, имя, контакты, Key Skills (amber glow-чипы), Soft
      Skills. Контентная колонка осталась светлой для максимальной
      читаемости при сканировании HR
- [x] Добавлено печатное правило: `.sidebar-dark` при печати принудительно
      становится белым с тёмным текстом (`!important` на всех потомках) —
      тёмный фон в PDF/на печати тратит тонер и часто вырезается браузером
- [x] Фото заменено на `myPhoto3.jpg` (автор сама подставила — более чистый
      кадр, без бейджа "1/2"), CSS-кроп (`scale`/`transformOrigin`)
      перенастроен под новые пропорции файла (1131×1461)
- [x] Обновлён `eslint-config-next` до `15.5.18` (совпадает с версией
      `next`) — автор одобрила. `lint` и `next build` проходят чисто
- [x] Проверено: desktop, мобильная ширина (390px), production-сборка с
      `basePath`

## 15. Фоновые градиентные шары (neon.com-style) — сделано

- [x] По вопросу "какие цвета нравятся HR": научно/практически самая
      "нанимаемая" палитра — тёмный/графитовый + белый + один тёплый
      акцент (ровно то, что уже было). Разноцветную палитру специально не
      вводила — вместо этого добавила декоративный, а не цветовой приём
- [x] Добавлены 3 размытых градиентных "шара" (amber/orange + один
      холодный slate/indigo для глубины) на фиксированном фоне с мягким
      дрейфом (`@keyframes`, `filter: blur`), вдохновлено neon.com.
      Скрыты при печати (`display: none` в `@media print` — чисто
      декоративны, не несут информации, тратили бы тонер)
- [x] Проверено: не мешают читаемости карточек (сильный blur + низкая
      opacity + белые карточки поверх), desktop и мобильная ширина,
      lint/build чистые

## 16. Выравнивание aside/main — сделано

- [x] Автор заметила: тёмный сайдбар и белая карточка About Me начинались
      на разной высоте. Причина — вступительный абзац висел над карточкой
      без рамки, добавляя лишнюю высоту сверху у `main`, пока `aside`
      начинался сразу с тёмного блока. Перенесла абзац внутрь карточки
      About Me как вводную строку — теперь обе колонки стартуют вровень
- [x] Заодно нашла и починила потерю контента: абзац был `hidden lg:block`
      и не показывался на мобильных вообще — теперь виден везде
- [x] Оформила абзац как акцентную "цитату": крупнее, amber-полоса слева,
      выделены "4+ years" и ключевые технологии

## 17. Аудит всех сайтов с github.com/HelgaAthame (2026-07-22) — сделано

Проверила вживую (Playwright, переход + консоль на ошибки) каждую ссылку на
проект с профиля. Автор попросила только продиагностировать и составить
список — чинить будет сама.

- [x] **PulseOps** — работает полностью (логин + hCaptcha как задумано)
- [x] **store-example7** — работает; при первой проверке корзина казалась
      пустой после добавления товара — оказалось, это артефакт тестирования
      (жёсткий `page.goto('/shopping-cart')` сбрасывает in-memory состояние
      корзины), а не баг сайта. При переходе по ссылке "Cart" в самом сайте
      (клиентский переход) товар в корзине корректно виден
- [x] **news-app-olga + newsapp-back** — полный цикл создание/удаление
      статьи через реальный UI работает (подтверждает, что фикс бэкенда из
      п.8 держится)
- [x] **bomberman2d**, **cadence-musical-instruments**,
      **graphiql-app-git-develop-killthecreator** — грузятся чисто, ошибок в
      консоли нет
- [x] **best-book-store.netlify.app** — грузится чисто, в консоли только
      безобидные info-логи про медленную сеть/fallback-шрифт
- [ ] **Sensy** (sensy-front-github.vercel.app) — логин и дашборд работают,
      но в консоли повторяющиеся 404 (примерно раз в 60 сек):
      `GET .../api/chat/Alert/message` и `.../api/chat/Notification/message`
      — похоже на нереализованный на бэкенде эндпоинт поллинга уведомлений
- [ ] **news-app-olga** — мелкая опечатка в диалоге подтверждения удаления
      статьи: "Area you sure you want to delete..." → должно быть
      "Are you sure..."
- [x] **nest-next-kappa / music-library-back** — было принципиально не
      работает: у serverless-функций Vercel read-only файловая система
      (`409 "File was not created"`). **Починено 2026-07-23**, см. п.19

## 18. Кандидаты на добавление в CV из github.com/HelgaAthame — сделано

Список публичных repo (не форков), которых ещё нет в CV/README:

- **`logic`** (обновлён 2024-11-16, без описания) и **`reactnative`**
  (обновлён 2024-05-17, без описания) — единственные два репозитория,
  которые по свежести и профилю похожи на потенциальные кандидаты, но у
  обоих нет описания и я не открывала код внутри, чтобы оценить готовность
  к показу HR
- Остальные непредставленные repo (`nodejs2023Q2-service`, `react`,
  `RSClone` — уже представлен как Bomberman через ссылку на
  `RSClone/tree/develop`, `websocket`, `CRUD-api`, `file-manager`,
  `rsschool-cv`, `Early-projects`) — старые учебные repo 2022-2023 годов,
  на вид отработанные школьные задания, а не отдельные проекты уровня
  портфолио; предлагаю не добавлять
- [x] Открыла оба репозитория:
  - **`logic`** — задеплоено (`logic-delta.vercel.app`, 200 OK), код есть
    (React/TS/SCSS). НО: это тестовое задание другой компании (в README
    дословно "Задание:", ссылка на их Figma-макет, требование "pixel
    perfect", условия "код пишите так, как вы бы писали на реальном
    проекте") — по сути демонстрирует навык, но выглядит как чужой
    рекрутинговый тест, а не собственный проект. Технически показать HR
    можно (демонстрирует pixel-perfect вёрстку и работу с API), но решение,
    уместно ли включать чужое тестовое задание в портфолио, оставляю за
    автором
  - **`reactnative`** — простое todo-приложение на Expo (добавление/
    удаление задач, 3 компонента), без описания, без задеплоенной web-версии
    (только `expo start`, HR не сможет открыть в браузере без локального
    запуска). Слишком маленький и без демо-ссылки — предлагаю **не
    добавлять** как есть
- [x] Автор решила: `reactnative` не добавлять, `logic` тоже не добавлять.
      Ни один из них никуда не был добавлен (проверено — не было ни в
      `HrCV.tsx`, ни в README профиля), так что откатывать нечего

## 19. Правки по найденным багам (2026-07-23) — сделано

Автор попросила почитить всё найденное в аудите (п.17) — сама чинить не
стала, попросила исправить.

- [x] **Sensy** — реализован недостающий бэкенд-эндпоинт
      `GET /api/chat/:chatType/message` (модуль `chat`, Prisma-модель
      `ChatMessage` + миграция), которого не было вообще — фронтенд опрашивал
      его (Alert раз в минуту, Notification раз в час) по спеке из
      `BACKEND_PLAN.md`, но бэкенд всегда отвечал 404. Задеплоено на Render
      (ветка `dev`), проверено вживую: маршрут отвечает 401 (требует токен)
      вместо 404 — значит, живой. Системные триггеры и ручной POST-эндпоинт
      из плана — по-прежнему не реализованы (не входило в размах этой правки),
      лента будет пустой, пока их нет
- [x] **News App** — опечатка "Area you sure" → "Are you sure" исправлена.
      Заодно нашла более серьёзную проблему: реальная продакшен-ветка на
      Vercel — `dev` (не `main`, как я думала изначально), и её билд-команда
      (`expo build:web`) уже давно не поддерживается текущим Expo CLI — из-за
      этого `news-app-olga.vercel.app` не пересобирался с ноября 2024 года,
      сколько бы коммитов ни уходило в `dev`. Обновила `vercel.json` и
      `package.json` на `dev` до рабочей команды (`expo export --clear`,
      как уже было на `main`), проверила деплой — ушёл в прод (`target:
      production`), опечатка подтверждена исправленной на живом сайте
- [x] **Music Library (nest-next-back)** — мигрировала `FileService` с
      локального диска на **Vercel Blob** (публичный store, создан и
      подключён к проекту через API/CLI — токен `BLOB_READ_WRITE_TOKEN`
      прописался в проект автоматически, от автора ничего не потребовалось).
      Заодно убрала `ServeStaticModule` и обвязку вокруг него (не нужна:
      существовала только ради раздачи файлов с локального диска). На
      фронтенде убрала склейку `NEXT_PUBLIC_BASE_URL + relative path` в 6
      местах — картинка/аудио теперь приходят готовым CDN-URL. Проверено
      сквозным тестом: реальная загрузка альбома с картинкой → `201`, файл
      публично доступен по Blob-ссылке, тестовые данные удалены после
      проверки
- [x] Полосатый фон CV (см. п.15, редизайн после neon.com) заменён на
      pastel-полосы с реакцией на курсор по отдельному запросу автора —
      см. п.20

## 20. Полосатый интерактивный фон CV (2026-07-23) — сделано

- [x] Заменила градиентные "шары" на фоне `/hr-cv` на диагональные пастельные
      полосы разной ширины (амбер/индиго/оранжевый/сланцевый — цвета уже
      использовавшейся палитры). Два слоя: приглушённый базовый всегда виден,
      более насыщенный слой открывается только в круге вокруг курсора через
      `mask-image`, повёрнутый на чуть другой угол — из-за рассинхрона углов
      создаётся ощущение лёгкого "искажения" под курсором без SVG-фильтров
- [x] Автор отметила два бага сразу после первого деплоя: (1) курсор-эффект
      срабатывал некорректно — не туда, куда наведён курсор; (2) полосы
      одного цвета были одинаковой ширины, попросила рандомизировать и
      сделать самую узкую — уже. Обе правки сделаны и задеплоены:
      - Причина бага с курсором: `mask-image`'s `at var(--x) var(--y)`
        считается от угла бокса самого элемента, а не от вьюпорта; слои с
        полосами были `inset: -15%` (сдвинуты от угла экрана "про запас" для
        поворота — это не нужно, `repeating-linear-gradient` сам заполняет
        бокс под любым углом без лишнего размера). Исправила на `inset: 0`
      - Ширина полос теперь не единая на цвет: 16-полосный паттерн с
        рандомизированной шириной от 8px (было единообразно ~70px)
- [x] Задеплоено и проверено — `inset:0` и рандомизированные ширины полос
      подтверждены прямо в собранном CSS на живом сайте. Playwright MCP не
      переподключился за сессию, поэтому визуально (скриншотом) не смотрела —
      если на деле эффект курсора всё ещё не тот, что ожидался, дай знать,
      что именно не так
- [x] Автор посмотрела вживую и попросила ещё три правки — все сделаны и
      задеплоены:
      - "Наклон полос при наведении некрасивый, пусть будет как лупа" —
        убрала разный угол у base/glow слоя (был 115°/124°, стало 115°/115°
        у обоих), эффект под курсором теперь через `background-size: 145%`
        (пятно читается как увеличенный кусок узора, а не перекошенный)
      - "Цвета как в цирке" — убрала полностью насыщенные amber-500/orange/
        indigo, оба слоя переведены на пастельные тона (100-200 диапазон
        Tailwind), после ещё одного прохода "слишком блёкло" — нашла
        среднее между версиями
      - "Радиус курсора огромный" — сначала ограничила маску `1cm`
        (радиус кружка вокруг курсора у полос), но осталось "всё равно
        огромный": нашла вторую причину — отдельный слой `.cursor-glow`
        (тёплая амбер-подсветка) жил своей жизнью с радиусом 560px (~15см!).
        Ограничила оба слоя до 1cm — CSS поддерживает физические единицы
        напрямую в `radial-gradient`, без пересчёта в px
      - "Расстояние между полосами огромное, полос мало, не хардкодь ширину
        руками — рандомизируй в пределах" — переписала: раньше ширины были
        вручную подобранные числа в CSS; теперь паттерн полос генерируется
        в `HrCV.tsx` детерминированным PRNG (mulberry32, не `Math.random()`
        — этот файл уже словил баг гидратации от недетерминированного фона,
        второй раз наступать не стала) по параметрам `{count, minWidth,
        maxWidth, minGap, maxGap, seed}` — зазоры сжаты с 28-62px до 4-14px,
        полос стало 28 вместо 16. CSS-класс теперь отвечает только за общую
        раскладку (позиция/маска/масштаб), сам градиент — inline-style из
        кода
      - Проверено: lint/prod-build чистые, в статическом экспорте оба слоя
        (`base`/`glow`) содержат одинаковый сгенерированный градиент —
        гидрационного расхождения нет

## 13. Требует твоего участия (не сделать за тебя)

- [x] Заменить аватар на github.com/settings/profile — сделано автором,
      проверила: теперь настоящее фото вместо AI-плейсхолдера
- [ ] Отозвать/переиздать GitHub Personal Access Token с урезанным скоупом
      (`repo` вместо полного набора прав, который был выдан по умолчанию) —
      github.com/settings/tokens. Аналогично проверить Vercel/MongoDB
      Atlas/Supabase токены — все рабочие, но стоит убедиться, что скоуп
      минимально достаточный. **Пропущено по просьбе автора (2026-07-15) —
      вернуться позже**
- [ ] В `nest-next` в истории git остался старый пароль от MongoDB
      (`olgafront:666@cluster0.jmctt.mongodb.net`) — сам код уже не
      использует его (заменено на env var), но строка навсегда видна в
      истории публичного репозитория. Если этот кластер ещё где-то
      используется — сменить пароль в MongoDB Atlas
- [ ] Удалить (по желанию) заброшенный дубль-проект `nest-next-d1o` в
      Vercel-панели (`nest-next-back.vercel.app`, 504, не используется —
      актуальный бэкенд теперь `music-library-back`)
- [ ] Подтвердить, есть ли фриланс/пет-проектный опыт фронтенда до CreativeIT
      (Aug 2021) — если да, скажи период, чтобы честно посчитать общий стаж
      отдельно от коммерческого
---

Инбокс — сюда автор добавляет пункты вручную, ассистент переносит их в план
выше и удаляет отсюда после обработки. Не удалять эту инструкцию, только
обработанные пункты.



